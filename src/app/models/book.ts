export interface Book{
    id: number;
    title: string;
    author: string;
    dateOfPublication: string;
    genre: string;
    available: boolean;
    imageURL: string;
    description: string;
}